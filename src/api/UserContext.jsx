import { App } from '@capacitor/app';
import React,{createContext,useContext,useState,useEffect}from'react';import{warn}from'../utils/logger.js';import{createAnimeVaultAuthClient}from'./authClient';import{clearActiveSubAccount}from'../utils/subAccounts';const authClient=createAnimeVaultAuthClient();import{fetchWatchHistory,addToHistory as dbAddToHistory,clearWatchHistory as dbClearWatchHistory,fetchContinueWatching,updateContinueWatching as dbUpdateContinueWatching,fetchLikedItems,toggleLikeItem as dbToggleLike,updateUserProfile as dbUpdateUserProfile,fetchReminders,addReminder as dbAddReminder,removeReminder as dbRemoveReminder,syncGoogleUserToDb,createUserSession,restoreSession,deleteUserSession,getUserStats,updateUserStats,getFavorites,toggleFavorite,setFavorite,getWatchHistory as dbGetWatchHistory,addWatchHistory as dbAddWatchHistory,getLevel,addXP,addActivity,fetchSubAccounts as dbFetchSubAccounts,createSubAccount as dbCreateSubAccount,updateSubAccount as dbUpdateSubAccount,deleteSubAccount as dbDeleteSubAccount,ensureMainSubAccount as dbEnsureMainSubAccount}from'./db';import{userLogin as dbUserLogin,userSignup as dbUserSignup}from'./authDb';import{proxyLogin,proxySignup,proxySyncAuthUser,proxyRestoreSession,proxyLogout,clearStoredProxyToken}from'./authProxy';const UserContext=createContext(null);const CACHED_USER_KEY='animevault_cached_user';const GUEST_USER={id:null,username:'Guest',avatar:null,banner:null,is_guest:true,is_admin:false,is_verified:false};
function normalizeProxyUser(proxyUser){if(!proxyUser)return null;const user=proxyUser.user||proxyUser;return{id:user.id||null,username:user.username||user.email||user.name||'User',avatar:user.avatar||user.image||null,banner:user.banner||null,is_admin:!!user.is_admin,is_verified:!!user.is_verified,is_guest:false}}
async function tryCreateUserSession(userId){try{return await createUserSession(userId)}catch(err){warn('[AnimeVault Auth] Persistent DB session creation failed:',err?.message||err);return null}}
export function UserProvider({children}){const[user,setUserState]=useState(null);const[authLoading,setAuthLoading]=useState(true);const setUser=next=>{setUserState(next);try{next?localStorage.setItem(CACHED_USER_KEY,JSON.stringify(next)):localStorage.removeItem(CACHED_USER_KEY)}catch{}};const[history,setHistory]=useState([]),[continueWatching,setContinueWatching]=useState([]),[likes,setLikes]=useState([]),[reminders,setReminders]=useState([]),[showAuthModal,setShowAuthModal]=useState(false),[authTab,setAuthTab]=useState('login'),[activeSubAccount,setActiveSubAccountState]=useState(null),[subAccounts,setSubAccounts]=useState([]);
 const loginWithDbCredentials=async(email,password)=>{const dbRes=await dbUserLogin(email,password);if(!dbRes.success)return{success:false,message:dbRes.message||'Login failed. Please check your credentials.'};await tryCreateUserSession(dbRes.user.id);setUser(dbRes.user);setShowAuthModal(false);return{success:true}};const signupWithDbCredentials=async(email,password)=>{const dbRes=await dbUserSignup(email,password);if(!dbRes.success)return{success:false,message:dbRes.message||'Signup failed. Please try again.'};await tryCreateUserSession(dbRes.user.id);setUser(dbRes.user);setShowAuthModal(false);return{success:true}};const loginAsGuest=async()=>{setUser({...GUEST_USER});setShowAuthModal(false);return{success:true,user:{...GUEST_USER}}};
 const sendEmailOtp=async email=>{try{if(!email?.trim())return{success:false,message:'Enter your email address first.'};const result=await authClient.emailOtp.sendVerificationOtp({email:email.trim(),type:'sign-in'});if(result?.error)return{success:false,message:result.error.message||'Could not send the verification code.'};return{success:true}}catch(err){warn('[AnimeVault Auth] OTP send failed:',err?.message||err);return{success:false,message:err?.message||'Could not send the verification code. Please try again.'}}};
 const loginWithEmailOtp=async(email,otp)=>{try{if(!email?.trim()||!otp?.trim())return{success:false,message:'Email and verification code are required.'};const result=await authClient.signIn.emailOtp({email:email.trim(),otp:otp.trim()});if(result?.error)return{success:false,message:result.error.message||'Invalid or expired verification code.'};const session=await Promise.race([authClient.getSession().catch(()=>null),new Promise(r=>setTimeout(()=>r(null),2500))]);const sessionUser=await syncAuthSessionUser(session?.data||result?.data);if(!sessionUser)return{success:false,message:'OTP verified, but the AnimeVault account could not be synchronized.'};setShowAuthModal(false);return{success:true,user:sessionUser}}catch(err){warn('[AnimeVault Auth] OTP sign-in failed:',err?.message||err);return{success:false,message:err?.message||'OTP sign-in failed. Please try again.'}}};
 const syncAuthSessionUser=async(existing=null)=>{const data=existing||((await authClient.getSession()).data);const currentUser=data?.user||data?.session?.user;if(!currentUser?.email)return null;const syncRes=await syncGoogleUserToDb(currentUser.email,currentUser.image||currentUser.avatar_url||null,currentUser.emailVerified||currentUser.email_verified||false);const sessionUser=syncRes.success?syncRes.user:{id:currentUser.id,username:currentUser.email||currentUser.name||'User',avatar:currentUser.image||currentUser.avatar_url||null,is_admin:false,is_verified:!!(currentUser.emailVerified||currentUser.email_verified),is_guest:false};setUser(sessionUser);setShowAuthModal(false);return sessionUser};
 const initSession=async()=>{setAuthLoading(true);setUserState(null);try{const auth=await Promise.race([authClient.getSession().catch(()=>null),new Promise(r=>setTimeout(()=>r(null),2500))]);const data=auth?.data;if(data?.session||data?.user){const u=await syncAuthSessionUser(data);if(u)return}const proxy=await Promise.race([proxyRestoreSession().catch(()=>({success:false})),new Promise(r=>setTimeout(()=>r({success:false}),1500))]);if(proxy.success&&proxy.user){setUser(normalizeProxyUser(proxy.user));return}const dbUser=await Promise.race([restoreSession().catch(()=>null),new Promise(r=>setTimeout(()=>r(null),1500))]);if(dbUser){setUser(dbUser);return}try{localStorage.removeItem(CACHED_USER_KEY)}catch{}setUserState(null)}catch(err){warn('[AnimeVault Auth] Session init failed:',err);setUser(null)}finally{setAuthLoading(false)}};
 useEffect(()=>{initSession();const subscription=authClient.onAuthStateChange?.((_event,session)=>{if(session?.user)syncAuthSessionUser({session,user:session.user}).finally(()=>setAuthLoading(false));else{setUser(null);setAuthLoading(false)}});return()=>{subscription?.data?.subscription?.unsubscribe?.()}},[]);
 const syncUserData=async(userId)=>{if(!userId)return;const[hist,cw,liked,rem]=await Promise.all([fetchWatchHistory(userId).catch(()=>[]),fetchContinueWatching(userId).catch(()=>[]),fetchLikedItems(userId).catch(()=>[]),fetchReminders(userId).catch(()=>[])]);setHistory(hist||[]);setContinueWatching(cw||[]);setLikes(liked||[]);setReminders(rem||[])};useEffect(()=>{if(user?.id)void syncUserData(user.id)},[user?.id]);
 const logout=async()=>{try{await authClient.signOut?.();await proxyLogout()}catch{}try{if(user?.id)await deleteUserSession(user.id)}catch{}clearActiveSubAccount();clearStoredProxyToken();setUser(null);setHistory([]);setContinueWatching([]);setLikes([]);setReminders([])};
 const fetchSubAccounts=async()=>{if(!user?.id)return[];const profiles=(await dbFetchSubAccounts(user.id).catch(()=>[]))||[];setSubAccounts(profiles);return profiles};const ensureMainSubAccount=async()=>user?.id?dbEnsureMainSubAccount(user.id).catch(()=>null):null;const createSubAccount=async data=>user?.id?dbCreateSubAccount(user.id,data).catch(e=>({success:false,message:e.message})):({success:false,message:'Not signed in'});const updateSubAccount=async(id,data)=>user?.id?dbUpdateSubAccount(user.id,id,data).catch(e=>({success:false,message:e.message})):({success:false,message:'Not signed in'});const deleteSubAccount=async id=>user?.id?dbDeleteSubAccount(user.id,id).catch(e=>({success:false,message:e.message})):({success:false,message:'Not signed in'});const addToHistory=async item=>{setHistory(p=>[item,...p.filter(x=>x.id!==item.id)]);if(user?.id)await dbAddToHistory(user.id,item).catch(()=>{})};const clearHistory=async()=>{setHistory([]);if(user?.id)await dbClearWatchHistory(user.id).catch(()=>{})};const updateContinueWatching=async item=>{setContinueWatching(p=>[item,...p.filter(x=>x.id!==item.id)].slice(0,50));if(user?.id)await dbUpdateContinueWatching(user.id,item).catch(()=>{})};const toggleLike=async item=>{setLikes(p=>p.some(x=>x.id===item.id)?p.filter(x=>x.id!==item.id):[...p,item]);if(user?.id)await dbToggleLike(user.id,item).catch(()=>{})};const isLiked=id=>likes.some(x=>x.id===id);const updateProfile=async data=>{setUser({...user,...data});if(user?.id)await dbUpdateUserProfile(user.id,data).catch(()=>{})};const addReminder=async item=>{setReminders(p=>[...p.filter(x=>x.id!==item.id),item]);if(user?.id)await dbAddReminder(user.id,item).catch(()=>{})};const removeReminder=async id=>{setReminders(p=>p.filter(x=>x.id!==id));if(user?.id)await dbRemoveReminder(user.id,id).catch(()=>{})};const isReminded=id=>reminders.some(x=>x.id===id);
 return <UserContext.Provider value={{user,authLoading,setUser,history,continueWatching,likes,reminders,subAccounts,activeSubAccount,showAuthModal,authTab,setShowAuthModal,setAuthTab,setActiveSubAccountState,fetchSubAccounts,ensureMainSubAccount,createSubAccount,updateSubAccount,deleteSubAccount,login:loginWithDbCredentials,signup:signupWithDbCredentials,loginAsGuest,sendVerificationCode:async()=>({success:false,message:'Use email OTP sign-in from the authentication screen.'}),sendEmailOtp,loginWithEmailOtp,logout,syncUserData,addToHistory,clearHistory,updateContinueWatching,toggleLike,isLiked,updateProfile,addReminder,removeReminder,isReminded}}>{children}</UserContext.Provider>}
export function useUser(){const context=useContext(UserContext);if(!context)throw new Error('useUser must be used within UserProvider');return context}
