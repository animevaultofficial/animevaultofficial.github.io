// Android now uses the same application shell as the web client.
// Keeping one source of truth prevents the native build from falling behind
// when web features, routes, authentication, or backend integrations change.
import App from '../App';

export default App;
