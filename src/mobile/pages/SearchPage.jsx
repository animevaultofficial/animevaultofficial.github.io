import React,{useEffect,useRef,useState}from'react';import{Search,X,SlidersHorizontal,TrendingUp,Star,Sparkles,ChevronDown}from'lucide-react';import{searchAnime as searchAnimeApi,fetchHomeData,getTitle,getImage}from'../api/anilist';
const GENRES=['Action','Adventure','Comedy','Drama','Fantasy','Horror','Mystery','Romance','Sci-Fi','Slice of Life','Sports','Supernatural','Thriller','Psychological'];const SORT_OPTIONS=[{value:'TRENDING_DESC',label:'Trending'},{value:'POPULARITY_DESC',label:'Popular'},{value:'SCORE_DESC',label:'Top Rated'},{value:'FAVOURITES_DESC',label:'Favorites'}];const STATUS_OPTIONS=[{value:'All',label:'All'},{value:'RELEASING',label:'Airing'},{value:'FINISHED',label:'Finished'},{value:'NOT_YET_RELEASED',label:'Upcoming'}];const CURRENT_YEAR=new Date().getFullYear();const YEARS=['All',...Array.from({length:20},(_,i)=>String(CURRENT_YEAR-i))];
function normalize(data){if(Array.isArray(data))return data.filter(Boolean);return data?.Page?.media||data?.media||data?.results||[]}
async function runSearch(query,genre,sort,status,year){try{return normalize(await searchAnimeApi(query,genre||null,sort,status,year))}catch(error){console.warn('[AnimeVault Mobile] search failed:',error?.message||error);return[]}}
function Poster({item,onClick}){const title=getTitle(item),image=getImage(item);const[failed,setFailed]=useState(false);return <button className="av-explore-card" type="button" onClick={()=>onClick(item.id)} aria-label={`Open ${title}`}><div className="av-explore-poster">{image&&!failed?<img src={image} alt={title} loading="lazy" onError={()=>setFailed(true)}/>:<div className="av-explore-fallback">A</div>}{item.averageScore!=null&&<span className="av-explore-score"><Star size={9} fill="currentColor"/>{item.averageScore}%</span>}</div><strong>{title}</strong><small>{item.format||'Anime'}{item.seasonYear?` · ${item.seasonYear}`:''}</small></button>}
export default function SearchPage({navigate}){const[query,setQuery]=useState(''),[results,setResults]=useState([]),[trending,setTrending]=useState([]),[loading,setLoading]=useState(false),[sort,setSort]=useState('TRENDING_DESC'),[genre,setGenre]=useState(''),[status,setStatus]=useState('All'),[year,setYear]=useState('All'),[filtersOpen,setFiltersOpen]=useState(false),[history,setHistory]=useState(()=>{try{const saved=JSON.parse(localStorage.getItem('av_search_history')||'[]');return Array.isArray(saved)?saved.slice(0,8):[]}catch{return[]}});const requestRef=useRef(0);
useEffect(()=>{let cancelled=false;(async()=>{try{const data=sort==='TRENDING_DESC'?await fetchHomeData():await runSearch('', '',sort,'All','All');const items=sort==='TRENDING_DESC'?normalize(data?.trending?.media||data?.trending):normalize(data);if(!cancelled)setTrending(items)}catch{if(!cancelled)setTrending([])}})();return()=>{cancelled=true}},[sort]);
useEffect(()=>{const q=query.trim();if(!q){setResults([]);setLoading(false);return}const timer=setTimeout(async()=>{const requestId=++requestRef.current;setLoading(true);const items=await runSearch(q,genre,sort,status,year);if(requestId!==requestRef.current)return;setResults(items);setLoading(false);if(items.length){setHistory(prev=>{const next=[q,...prev.filter(v=>v.toLowerCase()!==q.toLowerCase())].slice(0,8);try{localStorage.setItem('av_search_history',JSON.stringify(next))}catch{}return next})}},400);return()=>clearTimeout(timer)},[query,genre,sort,status,year]);
const openAnime=id=>id!=null&&navigate('anime-detail',{id});const searched=Boolean(query.trim());return <div className="av-explore"><section className="av-explore-head"><div><span className="av-explore-kicker"><Sparkles size={12}/> DISCOVER</span><h1>Explore Anime</h1><p>Find your next obsession.</p></div></section><div className="av-explore-search-wrap"><Search size={19}/><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>{if(e.key==='Escape')setQuery('')}} placeholder="Search anime, characters, titles..." aria-label="Search anime" autoCapitalize="none" autoCorrect="off"/>{query&&<button type="button" onClick={()=>setQuery('')} aria-label="Clear search"><X size={17}/></button>}<button type="button" className={filtersOpen?'active':''} onClick={()=>setFiltersOpen(v=>!v)} aria-label="Toggle filters"><SlidersHorizontal size={18}/></button></div>{filtersOpen&&<section className="av-explore-filters"><div className="av-filter-row"><span>Sort</span><div className="av-filter-scroll">{SORT_OPTIONS.map(o=><button key={o.value} className={sort===o.value?'active':''} onClick={()=>setSort(o.value)}>{o.label}</button>)}</div></div><div className="av-filter-row"><span>Status</span><div className="av-filter-scroll">{STATUS_OPTIONS.map(o=><button key={o.value} className={status===o.value?'active':''} onClick={()=>setStatus(o.value)}>{o.label}</button>)}</div></div><div className="av-filter-row"><span>Year</span><div className="av-filter-scroll">{YEARS.map(y=><button key={y} className={year===y?'active':''} onClick={()=>setYear(y)}>{y}</button>)}</div></div><div className="av-filter-row genre-row"><span>Genre</span><div className="av-filter-scroll">{GENRES.map(g=><button key={g} className={genre===g?'active':''} onClick={()=>setGenre(genre===g?'':g)}>{g}</button>)}</div></div></section>}{!searched&&history.length>0&&<section className="av-explore-section av-history"><header><h2>Recent searches</h2><button onClick={()=>{setHistory([]);localStorage.removeItem('av_search_history')}}>Clear</button></header><div>{history.map(item=><button key={item} onClick={()=>setQuery(item)}>{item}<Search size={12}/></button>)}</div></section>}{searched?<section className="av-explore-section"><header><div><h2>{loading?'Searching…':`${results.length} results`}</h2><p>{query.trim()}</p></div></header>{loading?<div className="av-explore-grid">{Array.from({length:6},(_,i)=><div className="av-explore-skeleton" key={i}/>)}</div>:results.length?<div className="av-explore-grid">{results.map(item=><Poster key={item.id} item={item} onClick={openAnime}/>)}</div>:<div className="av-explore-empty"><Search size={34}/><h3>No anime found</h3><p>Try another title, genre, or year.</p></div>}</section>:<section className="av-explore-section"><header><div><h2><TrendingUp size={18}/> Trending now</h2><p>What everyone is watching</p></div><button className="av-sort-mini" onClick={()=>setFiltersOpen(true)}>{SORT_OPTIONS.find(o=>o.value===sort)?.label}<ChevronDown size={13}/></button></header><div className="av-explore-grid">{trending.slice(0,12).map(item=><Poster key={item.id} item={item} onClick={openAnime}/>)}</div></section>}</div>}
